# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Data artifacts (local databases, downloaded data, trained models)
*.sqlite
*.sqlite3
*.db
# Everything under data/ is generated or downloaded: databases, the Betfair
# hub CSVs (re-fetch with `furlong import-betfair-hub --download`), reports,
# published suggestions and model artifacts. None of it belongs in the repo,
# and the hub files are Betfair's to distribute, not ours.
data/
!data/.gitkeep
models/artifacts/
*.parquet

# Secrets / local config
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
